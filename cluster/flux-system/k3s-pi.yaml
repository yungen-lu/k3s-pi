---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: infra
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./cluster/infra
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
  # decryption:
  #   provider: sops
  #   secretRef:
  #     name: sops-gpg
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: app
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./cluster/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
  # decryption:
  #   provider: sops
  #   secretRef:
  #     name: sops-gpg
